title: Script Notes
description: 使ったことのあるスクリプトを整理して公開する個人サイト
lang: ja
timezone: Asia/Tokyo
url: "https://Mr-129.github.io"
baseurl: "/homepage"
markdown: kramdown
permalink: pretty
author: Mr-129
theme_stylesheet: /assets/css/themes/copper-file.css

collections:
  scripts:
    output: true
    # :path を使うと _scripts 配下のサブフォルダ構成まで URL に含まれる。
    # これで記事数が増えても、同名ファイル衝突を避けながら整理できる。
    permalink: /scripts/:path/

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: page
  - scope:
      path: ""
      type: "scripts"
    values:
      layout: script

plugins:
  - jekyll-feed
  - jekyll-seo-tag

exclude:
  - README.md
  - templates/
  - docs/
  - draft-private/
